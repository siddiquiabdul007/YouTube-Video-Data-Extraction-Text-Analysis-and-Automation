# import the subprocess module
import subprocess

# execute the combined.py file using the python interpreter
subprocess.run(["python", "combined.py"])

# execute the text-analysis.py file using the python interpreter
subprocess.run(["python", "text-analysis.py"])
